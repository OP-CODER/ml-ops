apiVersion: v1
kind: Service
metadata:
  name: iris-service
spec:
  type: LoadBalancer
  selector:
    app: iris        # Must match the pod label in your deployment
  ports:
    - name: http
      port: 80       # External port for the service
      targetPort: 8000  # Matches containerPort in your deployment
