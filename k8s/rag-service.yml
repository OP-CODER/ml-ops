apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: default
  labels:
    app: rag
spec:
  selector:
    app: rag
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
